
## Application Config
server.servlet.context-path=/quiz-service
server.port=8081
#server.port=9191

##JWT token Secret
jwt.secret=vsdsquizsecret

## Authentication
auth.secret=SOME-SECRET
auth.server=http:localhost:8081
client.id=SOME-CLIENT-ID

## AUTH 2.0 SECURITY KEYS
security.oauth2.client.clientId=my-client
security.oauth2.client.clientSecret=my-secret
security.oauth2.resource.tokenInfoUri=http://localhost:8080/quiz-service/token
security.oauth2.resource.preferTokenInfo=true
swagger.oauth.url=http://localhost:8080/quiz-service/authorize


##### Database Configuration
spring.datasource.url = jdbc:mysql://localhost:3306/quizdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username = root

## VS Specific
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.password = Elephant!

## DS Specific
spring.datasource.password = root

## Hybernate Config
## Hibernate Properties
## The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect